[project]
name = "backtest"
version = "0.1.0"
description = "Position-based daily stock factor backtesting framework (IC, group tests, long-short, top-N portfolios)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.11.2",
    "numpy>=2.5.3",
    "openpyxl>=3.1.5",
    "pandas>=2.2,<3",
    "pyarrow>=25.0.1",
    "scipy>=1.18.1",
    "statsmodels>=0.15.0",
]
